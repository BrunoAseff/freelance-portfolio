export const metadata = {
  title: "Estética",
  description: "Landing page de estética",
  icons: {
    icon: "/faviconEstetica.png",
  },
};

export default function DashboardLayout({ children }) {
  return <section>{children}</section>;
}
